# Application Configuration
spring.application.name=${SPRING_APPLICATION_NAME:foodly-backend}

# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/foodly_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:ayushagarwal}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:ayush1}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.dialect=${SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
spring.jpa.properties.hibernate.format_sql=${SPRING_JPA_PROPERTIES_HIBERNATE_FORMAT_SQL:true}

# Server Configuration
server.port=${SERVER_PORT:8080}

# JWT Configuration
jwt.secret=${JWT_SECRET:bQtieT1EPjAdzhUzdn6xEpFiPgxOmzWPDI4w0t/hygyU07o2Hx0TKJX5IcytXtKqNH2NUBLluXb8Qw9HTpStjw==}
jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,https://foodly11.netlify.app}
